---
import '../styles/global.css';

interface Props {
  title?: string;
}

const { title = 'QS 學術與雇主問卷聯絡人提報系統' } = Astro.props;
const base = import.meta.env.BASE_URL;
---

<!doctype html>
<html lang="zh-Hant">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="佛光大學 QS Academic & Employer Survey 聯絡人提報系統" />
    <link rel="icon" type="image/png" href={`${base}images/fgu-logo.png`} />
    <title>{title}</title>
  </head>
  <body class="min-h-screen text-slate-800 antialiased">
    <slot />
  </body>
</html>
